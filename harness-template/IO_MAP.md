# 输入输出地图（IO MAP）
> 从哪里读取输入。往哪里写输出。
> 安装 Harness 时，填写括号内的内容。

---

## 输入 — 从哪里读

### 配置文件

> 【列出智能体可能需要读取的所有配置来源。
>  每一项：路径 / 位置、格式、它控制什么。】

| 配置项 | 位置 | 格式 | 控制什么 |
|--------|------|------|---------|
| 【配置名】 | 【路径或环境变量】 | 【JSON/YAML/env/…】 | 【影响的内容】 |

### 运行时上下文（计算得出，不是文件）

> 【智能体需要知道的、在运行时派生的信息，不存储在文件中。
>  示例：当前用户、环境名称、功能开关、Git 分支。】

| 信息 | 如何获取 |
|------|---------|
| 【上下文项】 | 【获取方式】 |

### 环境变量

> 【列出项目使用的所有环境变量。标注哪些是密钥。】

```
【变量名】     — 【用途】【如果是密钥请标注"（密钥）"】
【变量名】     — 【用途】
```

---

## 输出 — 往哪里写

### 代码改动

> 【对于智能体可能新增的每种能力类型，给出精确的目录和文件命名规范。】

| 新增内容 | 写在这里 | 文件命名规范 |
|---------|---------|------------|
| 【例：新增服务】 | 【路径】 | 【命名规范】 |
| 【例：新增命令】 | 【路径】 | 【命名规范】 |
| 【例：新增类型】 | 【路径】 | 【命名规范】 |

**注册文件**（实现工作完成后最后处理）：

| 新增内容 | 注册文件 | 如何添加 |
|---------|---------|---------|
| 【例：新增工具】 | 【注册文件路径】 | 【加入数组 / 映射 / 导入】 |

### 后台任务输出

> 【如果项目运行后台任务，描述它们的输出位置。】

```
任务输出路径：【路径模式，例：~/.app/tasks/<taskId>.log】
格式：        【只追加文本 / JSON Lines / 二进制】
断点续读约定：【例：追踪字节偏移量，使用游标文件】
```

### 状态变更

> 【描述状态修改的约定。这是强制不可变性的关键。】

```
正确模式：
  【例：setAppState(prev => ({ ...prev, field: newValue }))】

禁止的反模式：
  【例：prev.field = newValue  ← 永远不要这样做】
```

---

## 生命周期钩子 / 事件系统

> 【如果项目有钩子、事件或中间件，在这里描述契约。】

### 钩子 / 事件契约

| 字段 | 类型 | 说明 |
|------|------|------|
| 输入（stdin/参数） | 【类型】 | 【钩子收到的内容】 |
| 输出（stdout/返回值） | 【类型】 | 【钩子必须返回的内容】 |
| 错误处理 | — | 【钩子失败时的处理方式】 |
| 超时 | 【时长】 | 【超时后强制终止】 |

### 新增一个钩子事件

```
1. 在【事件类型联合 / 枚举文件】中添加事件名
2. 在生命周期对应的位置调用【触发函数】
3. 在本文件中记录输入数据的结构
```

---

## 禁止写入文件的内容

> 【明确的禁止项——看起来合理但绝对不能做的事。】

- 密钥 / 凭证（使用环境变量或密钥管理器）
- 【项目特定禁止项2】
- 【项目特定禁止项3】
