# 项目引导（BOOTSTRAP）
> 为全新智能体提供项目定向信息。
> 为项目安装此 Harness 时，填写每一个括号内的内容。

---

## 这是什么项目？

> 【2–4 句话。它做什么？用什么运行时 / 语言？当前处于什么状态？
>  包括智能体必须提前知道的"特殊背景"，例如：这是从源码重建的项目、存在兼容性垫片等。】

---

## 入口文件

> 【列出新智能体应该优先阅读的 5–10 个文件，按优先级排序。
>  每个文件：路径 + 一句话说明它的职责。】

| 文件 | 职责 |
|------|------|
| 【路径】 | 【职责】 |

---

## 目录 / 模块地图

> 【画出目录树。每个顶层文件夹用一句话说明里面放的是什么。】

```
【项目根目录】/
  【目录1】/    【这里放什么】
  【目录2】/    【这里放什么】
  ...
```

---

## 语言、风格与约定

> 【填写智能体必须遵守的规则，以便产出风格一致的代码。
>  可以直接从 AGENTS.md / CONTRIBUTING.md 复制粘贴。】

- **语言 / 运行时**：【例：TypeScript + Bun、Python 3.12、Go 1.23】
- **代码风格**：【例：不加分号，单引号，函数用 camelCase，类用 PascalCase】
- **模块系统**：【例：ESM、CommonJS、Go modules】
- **文件大小目标**：【例：通常 < 400 行，最大 800 行】
- **命名规范**：【例：目录用 kebab-case，类文件用 PascalCase】
- **特殊规则**：【例：「有注释警告的文件中，不要重新排列 import 顺序」】

---

## 开发命令

> 【安装、构建、测试、冒烟测试的精确命令。
>  智能体用这些命令来验证自己的改动。】

```bash
【安装命令】       # 安装依赖
【构建命令】       # 编译 / 类型检查
【测试命令】       # 运行测试
【冒烟测试命令】   # 最快的基本验证
```

**最低验证标准**：每次改动后，`【冒烟测试命令】` 必须以 exit 0 结束。

---

## 按任务类型决定先读什么

> 【将常见任务类型映射到需要优先阅读的 2–3 个文件。
>  防止智能体读遍整个代码库。】

| 任务类型 | 先读这些文件 |
|---------|------------|
| 【任务类型1】 | 【文件1】、【文件2】 |
| 【任务类型2】 | 【文件1】、【文件2】 |

---

## 已知的坑 / 特殊说明

> 【列出会让有经验的工程师感到意外的内容。
>  垫片、回退逻辑、循环依赖、有意为之的临时方案等。】

- 【坑1】
- 【坑2】
